spring:
  datasource:
    username: test
    password: test
  jpa:
    properties.hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
      show_sql: true
      format_sql: true
      hbm2ddl.auto: validate